export enum ApiRoutes {
  Product = "/products/[id]",
  Categories = "/categories",
  Ingredients = "/ingredients",
  ProductsSearch = "/products/search",
}

export const Routes = {
  Checkout: "/checkout",
  Product: {
    Details: "/product/[id]",
  },
};
