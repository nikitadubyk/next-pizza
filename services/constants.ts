export enum ApiRoutes {
  Product = "/products/[id]",
  Categories = "/categories",
  Ingredients = "/ingredients",
  ProductsSearch = "/products/search",
}

export const Routes = {
  Product: {
    Details: "/product/[id]",
  },
};
